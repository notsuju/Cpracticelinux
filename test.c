#include <stdio.h>
#include <string.h>

int main() {
    // char ch;

    // printf("Enter a single character: ");
    // scanf(" %c", &ch);

    // printf("You entered: %c\n", ch);
    int x;
    printf("Enter a number : ");
    int y = scanf("%d", &x);
    printf("%d\n", y);
    return 0;
}